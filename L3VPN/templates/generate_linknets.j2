{% for subnet in linknet.available_subnets %}
    {{ subnet }} gives {{ subnet|ipsubnet(linknet.size) }} /{{ linknet.size }} subnets

    - {{ subnet|ipsubnet(linknet.size,0) }}
    - {{ subnet|ipsubnet(linknet.size,1) }}
    - {{ subnet|ipsubnet(linknet.size,2) }}
    - {{ subnet|ipsubnet(linknet.size,3) }}
{% endfor %}